import React from 'react';
import {ReactComponent as Spinner} from './../../assests/svg/spinner.svg';

const LoadingSpinner = () => {
    return (
       <Spinner/>
    );
};

export default LoadingSpinner;